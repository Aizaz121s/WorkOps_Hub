services:
  titan-hrms-backend:
    build: ./backend
    container_name: titan-hrms-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    volumes:
      - titan_hrms_data:/app/data

volumes:
  titan_hrms_data:
